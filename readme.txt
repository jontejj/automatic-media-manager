Requirements:

php extensions:
CURL
GD2
mysqli


Note this system uses the database name movies so 
if you already have that database name in use, 
change the name in database.sql

enter phpmyadmin and import the sql file design/database.sql

make sure that the database named movies has been created